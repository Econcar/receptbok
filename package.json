{
  "name": "receptbok",
  "version": "0.1.0",
  "private": true,
  "description": "Hushållets receptsamling: importera, laga efter, och handla till.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node scripts/check-syntax.mjs",
    "test": "npm run check && node --test \"tests/*.test.mjs\"",
    "predeploy": "node scripts/predeploy.mjs"
  }
}
